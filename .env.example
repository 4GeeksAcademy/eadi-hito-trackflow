# TrackFlow environment variables
# Este proyecto de tipos y utilidades de dominio no requiere variables de entorno
# para ejecutarse en TypeScript, pero se documenta la convención general para
# evitar archivos locales sensibles en el repositorio.

# Ejemplo:
# API_BASE_URL=http://localhost:3000
